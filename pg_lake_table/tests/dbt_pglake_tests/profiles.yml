dbt_pglake_tests:
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 25778 # must be same as Postgres test cluster port
      user: postgres
      pass: postgres
      dbname: postgres
      schema: public
      threads: 4
  target: dev
